# Data_Base — Agent Notes

## Build / test / lint
- No manifest present (no package.json, go.mod, pyproject.toml, or Cargo.toml) — no build/test/lint commands exist.
- Only command with direct evidence (README.md line 58): `docker compose -f MongoDB.yaml up` (run from `Docker/How to install mongo /`).

## Rules
- TODO: verify — no repo-specific rules (e.g. package manager, generated-file restrictions) were found in any file read.

## Read first
1. `README.md`
2. `DATA BASE/HR.sql`
3. `Docker/Docker.md`

Architecture: see ARCHITECTURE.md — read before structural changes
